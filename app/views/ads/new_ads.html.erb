<h1>Listing new ads</h1>

<table>
  <tr>
    <th>Description</th>
    <th>Name</th>
    <th>Status</th>
    <th>Type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ads.each do |ad| %>
  <tr>
    <td><%= ad.description %></td>
    <td><%= ad.name %></td>
    <td><%= ad.state_name %></td>
    <td><%= get_name_of_type(@types, ad.ads_type_id.to_i)%></td>
    <td><%= link_to 'Show', ad %></td>
    <td><%= link_to 'Deny', admin_actions_deny_path(:id => ad.id) %></td>
    <td><%= link_to 'Approve', admin_actions_approve_path(:id => ad.id) %></td>
  </tr>
<% end %>
</table>
<%= will_paginate @ads %>
<br />
